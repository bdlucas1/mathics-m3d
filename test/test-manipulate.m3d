``` m3d test:3d-sinsqrt
Manipulate[
    Plot3D[
        Sin[(x^2+y^2)*freq] / Sqrt[x^2+y^2+1] * amp, {x,-3,3}, {y,-3,3},
        PlotRange -> {-0.5,0.7}
    ],
    {{freq,1.0}, 0.1, 2.0, 0.1}, (* freq slider spec *)
    {{amp,1.0}, 0.0, 2.0, 0.1}  (* amp slider spec *)
]
```

``` m3d test:2d-sin
Manipulate[
    Plot[Sin[x*f], {x,0,10}, PlotPoints->10],
    {{f,1.0}, 0.1, 2.0, 0.2}
]
```

``` m3d test:3d-sincos
Manipulate[
    Plot3D[
        Sin[x]*Cos[y]*a,
        {x,0,10}, {y,0,10},
        Axes -> {True,True,True},
        PlotRange -> {-2,2}
    ],
    {{a,1}, 0, 2, 0.1}
]
```

``` m3d test:1d-numberline
Manipulate[
    NumberLinePlot[{1,x,4}],
    {{x,2}, 1.0, 4.0, 0.1}
]
```

``` m3d test:2d-polar
(* TODO: axes should be centered; maybe use plotly native polar plot for this? *)
(* Interactive speed is marginal for this plot *)
Manipulate[
    PolarPlot[
        Sqrt[t*a],
        {t, 0, 16 Pi},
        PlotRange -> {{-8,8}, {-8,8}}
    ],
    {{a,1}, 0.7, 1.3, 0.01}
]
```

``` m3d test:3d-sincos-max
Manipulate[
    Plot3D[
        Sin[x]*Cos[y],
        {x,0,xmax},
        {y,0,ymax}
    ],
    {{xmax,1},0,10,0.1},
    {{ymax,1},0,10,0.1}
]
```

``` m3d test:2d-if
Manipulate[
    Plot[
        If[x > threshold, x, -x],
        {x,-1,1}
    ],
    {{threshold,0},-1,1,0.05}
]
```

``` m3d test:3d-eex
Manipulate[
    Plot3D[
        {Exp[Exp[x + I y]], z},
        {x,-1.5,1.5}, {y,-6,6},
        PlotRange->{-1,1.5},
        BoxRatios->{1,1,1}, ViewPoint->{-2,-0.7,.7}
    ],
    {{z,1}, -1, 1.5, 0.05}
]
```

``` m3d test:fp
Manipulate[
    Plot3D[
        FractionalPart[x y],
        {x,-l,l}, {y,-l,l}
    ],
    {{l,1.5},0.95,2,0.05}
]
```

``` m3d test:2d-density
Manipulate[
    DensityPlot[
        Sin[a x] Cos[b y], {x,0,10 Pi}, {y,0,5 Pi}
    ],
    {{a,1}, 0.5, 2, 0.05},
    {{b,1}, 0.5, 2, 0.05}
]
```

``` m3d test:complex
(* Use the sliders to move the poles and zeros *)
Manipulate[
    ComplexPlot[
        ((z + I - z1) (z - I - z2)) / ((z - 1 + I p1) (z + 1 + I p2)),
        {z, -2 - 2 I, 2 + 2 I}
    ],
    {{z1,0}, -1.0, 1.0, 0.05},
    {{z2,0}, -1.0, 1.0, 0.05},
    {{p1,0}, -1.0, 1.0, 0.05},
    {{p2,0}, -1.0, 1.0, 0.05}
]
```

``` m3d test:complex-eex
Manipulate[
    ComplexPlot[
        Exp[Exp[z]]-k,
        {z,-1-10 I,3+10 I},
        PlotPoints->{400,400}
    ],
    {{k,1},0,2,0.05}
]
```

``` m3d test:2d-zeta
Manipulate[
    Plot[
        Abs[Zeta[x I + y]],
        {x,0,50}, PlotRange->{0,4}
    ],
    {{y,0.5},0,.95,0.05}
]
```

