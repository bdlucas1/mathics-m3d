``` m3d test:manipulate-plot3d-module
Manipulate[
    Plot3D[
        Module[
            {z = 0, i, freq, amp},
            freq = 1;
            amp = 1;
            For[i = 1, i <= n, i++,
                z += amp * Sin[freq x] * Cos[freq y];
                freq *= fmul;
                amp /= adiv;
            ];
            z
        ],
        {x, -Pi, Pi}, {y, -Pi, Pi},
        Mesh -> None
    ],
    {{n,6}, 1, 10, 1},
    {{fmul,2}, 1, 3, 0.1},
    {{adiv,2}, 1, 3, 0.1}
]
```

``` m3d test:plot3d-module
Plot3D[
    Module[
        {z = 0, i, freq, amp, fmul=1, adiv=1, n=3},
        freq = 1;
        amp = 1;
        For[i = 1, i <= n, i++,
            z += amp * Sin[freq x] * Cos[freq y];
            freq *= fmul;
            amp /= adiv;
        ];
        z
    ],
    {x, -Pi, Pi}, {y, -Pi, Pi},
    Mesh -> None
]
```

