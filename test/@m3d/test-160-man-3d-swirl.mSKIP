Manipulate[
    Plot3D[
        (* TODO: ArcTan[x] works, ArcTan[x,y]  does not *)
        Sin[5 Sqrt[x^2 + y^2] + n ArcTan[x, y]] / (1 + (x^2 + y^2)),
        {x, -2, 2}, {y, -2, 2},
        PlotRange -> {System`Automatic, System`Automatic, {-2,2}},
        ColorFunction -> "Rainbow"
    ],
    {{n,3},0,5,1}
]
