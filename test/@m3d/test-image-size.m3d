Basic size
``` m3d test:basic
ContourPlot[x^2 - y^2, {x, -2, 2}, {y, -2, 2}]
```

Grid - half size, except one which is explicit
``` m3d test:grid
Grid[{
    {
        ContourPlot[x^2 - y^2, {x, -2, 2}, {y, -2, 2}],
        ContourPlot[{x^2+y^2 == 1, x^2-y^2==0.5} , {x,-2,2},{y,-2,2}, ImageSize->100]
    },
    {
        ContourPlot[{x^2+y^2 == 1, x^2-y^2==0.5} , {x,-2,2},{y,-2,2}],
        ContourPlot[x^2 - y^2, {x, -2, 2}, {y, -2, 2}]
    }
}]
```

Basic size again

``` m3d test:basic-again
ContourPlot[x^2 - y^2, {x, -2, 2}, {y, -2, 2}]
```

Row - quarter size
``` m3d test:row
Row[{
    ContourPlot[x^2 - y^2, {x, -2, 2}, {y, -2, 2}],
    ContourPlot[{x^2+y^2 == 1, x^2-y^2==0.5} , {x,-2,2},{y,-2,2}]
}]
```

List - want half size, but is in RowBox - how to handle...
Also, funkiness with LaTeX...
``` m3d test:list-is-broken
{
    ContourPlot[x^2 - y^2, {x, -2, 2}, {y, -2, 2}],
    ContourPlot[{x^2+y^2 == 1, x^2-y^2==0.5} , {x,-2,2},{y,-2,2}]
}
```

Explicit, just a number, 100

``` m3d test:explicit-number
ContourPlot[x^2 - y^2, {x, -2, 2}, {y, -2, 2}, ImageSize->100]
```

Explicit, not square, tall and thin
``` m3d test:not-square
ContourPlot[x^2 - y^2, {x, -2, 2}, {y, -2, 2}, ImageSize->{100,200}]
```
